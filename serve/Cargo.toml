[package]
name = "serve"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.6.20"
matx_dev = { path = ".." }
tokio = { version = "1.33.0", features = ["full"] }
tower-http = { version = "0.4.4", features = ["fs"] }
